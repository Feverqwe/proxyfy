import {Button} from '@mui/material';
import {styled} from '@mui/system';

import MyButton from './MyButton';

const MyButtonM = styled(MyButton)({
  margin: 0,
});

export default MyButtonM as typeof Button;

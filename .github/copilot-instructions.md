# GitHub Copilot instructions

Follow [`../AGENTS.md`](../AGENTS.md) as the canonical project guide.

In particular: keep changes scoped, never edit generated `dist/` files, preserve Chrome extension
runtime boundaries, add tests for behavior changes, and run the relevant tests plus `npm run lint`
before handoff. Update `AGENTS.md`, rather than this file, when project guidance changes.
